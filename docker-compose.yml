

services:
  unbound:
    image: tiredofit/unbound:latest
    platform: linux/arm64 # platform for mac, see README for info
    container_name: unbound
    restart: unless-stopped
    ports:
      - "5335:53/udp"
    environment:
      - LISTEN_PORT=5335
      - CUSTOM_RESOLVER_CONF=/etc/unbound/unbound.conf
    volumes:
      - ./unbound-conf/unbound.conf:/etc/unbound/unbound.conf:ro
      - ./unbound-conf/root.hints:/etc/unbound/root.hints:ro
      - ./unbound-conf/root.key:/etc/unbound/root.key:ro
    networks:
      pihole_net:
        ipv4_address: 172.22.0.10

  tailscale:
    image: tailscale/tailscale:latest
    platform: linux/arm64 # platform for mac, see README for info
    container_name: tailscale-pihole
    hostname: pihole-server
    environment:
      - TS_AUTHKEY=${AUTHKEY}  # Replace with your key
      - TS_EXTRA_ARGS=--accept-dns=false --advertise-tags=tag:dns
      - TS_STATE_DIR=/var/lib/tailscale
    volumes:
      - ./tailscale-data:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    restart: unless-stopped
    networks:
      pihole_net:
        ipv4_address: 172.22.0.20

  pihole:
    image: pihole/pihole:latest
    platform: linux/arm64 # platform for mac, see README for info
    container_name: pihole
    depends_on:
      - unbound
      - tailscale
    network_mode: "service:tailscale"
    environment:
      TZ: 'Asia/Kolkata' # Set to your local Timezone for correct Dates in pihole
      FTLCONF_webserver_api_password: "admin123" # set to "" if you do not want a password
      DNSMASQ_LISTENING: all
      PIHOLE_DNS_: 172.22.0.10#5335
    volumes:
      - ./etc-pihole:/etc/pihole
      - ./etc-dnsmasq.d:/etc/dnsmasq.d
    cap_add:
      - NET_ADMIN
    restart: unless-stopped

networks:
  pihole_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.22.0.0/24

volumes:
  tailscale-data:
    driver: local


